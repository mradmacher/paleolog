<a class="ui huge primary button" href="<%= project_species_path(@project) %>">Species</a>
<a class="ui huge primary button" href="<%= occurrences_path(@project) %>">Count</a>
<a class="ui huge primary button" href="<%= reports_path(@project) %>">Reports</a>
